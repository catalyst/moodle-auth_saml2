# .github/workflows/ci.yml test 2
name: ci

on: [push, pull_request]

jobs:
  test:
    uses: catalyst/catalyst-moodle-workflows/.github/workflows/ci.yml@cached-db
    secrets:
      moodle_org_token: ${{ secrets.MOODLE_ORG_TOKEN }}
    with:
      min_php: '7.4'
      internal_workflow_branch: 'cached-db'
