# .github/workflows/ci.yml Test 7
name: ci

on: [push, pull_request]

jobs:
  ci:
    uses: catalyst/catalyst-moodle-workflows/.github/workflows/ci.yml@ci-individual-jobs
    secrets:
      moodle_org_token: ${{ secrets.MOODLE_ORG_TOKEN }}
    with:
      internal_workflow_branch: ci-individual-jobs
